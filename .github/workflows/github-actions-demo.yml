name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.x' # Specify your .NET version

      - name: run dotnet tool
        run: dotnet --version

      #- name: Make tool executable (if needed)
      #  run: chmod +x ./tools/mybuildtool.dll # Add this line if your tool needs execution permissions

      - name: Run the custom .NET tool
        run: dotnet run ./tools/mybuildtool.dll ARGUMENTS # Replace `ARGUMENTS` with any required arguments
